# _inbox/

The "I do not know where this goes" pile. Drop anything here and mention it — it gets sorted
into the right folder, or thrown away if the site has no slot for it.

Nothing in here is imported by `pnpm media:check --import`; a file only reaches the site once
it sits in a real slot folder under a name that slot expects.
