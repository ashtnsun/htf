import { PhotoHero } from "@/components/home/heroes/PhotoHero";

/**
 * Home hero: the organization photo (heroes/PhotoHero). The other variants in heroes/ are no
 * longer reachable since the Shift + M menu was removed (2026-09-15).
 */
export function Hero() {
  return <PhotoHero />;
}
